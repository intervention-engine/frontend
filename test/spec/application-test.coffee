#
# describe 'Testing Application Loads', ->
#   describe 'The title should appear', ->
#     it 'should say Patient Filter', ->
#       visit('/').then ->
#         find('h1').text().should.include("Patient Filter")
